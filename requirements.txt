fastapi
uvicorn[standard]

sqlalchemy
alembic
psycopg2-binary

pydantic[email]
pydantic-settings
python-dotenv

redis
httpx
python-multipart
python-jose[cryptography]
bcrypt

# Observability
sentry-sdk[fastapi]
loguru
prometheus-fastapi-instrumentator

# Phase 2 — Route Optimization
ortools>=9.8

# Phase 3 — Scheduling
apscheduler>=3.10

# Phase P — Excel Export / Import
openpyxl>=3.1

# Phase 4 — Celery task queue (webhook delivery, marketplace matching)
celery>=5.3
flower>=2.0

# Phase 2 — LLM / LangChain
langchain-core>=0.2
langchain-google-genai>=1.0
langchain-openai>=0.1
langchain-anthropic>=0.1
langgraph>=0.1
